# 审计验收清单（v0.72.32 复审计，原始 v0.62.0 基线）

## 进度结论

- 当前项目处于“可测试 core/CLI 骨架 + smoke/proxy 生成验证链 + P1 契约闭环 + 独立 conda 环境验证 + P2 可交互 PySide6 配置页 + GUI 内同步执行/刷新/输出摘要查看 + P6 维护性收敛 + P3 checkpoint inference artifact contract gate + generation backend compatibility gate、inference architecture/condition contract gate + PyTorch diffusion smoke checkpoint inference planning contract + production training dataset contract + training index JSONL 证据核对 + training objective/loss/QC mapping contract + production training plan artifact + P4 可恢复 tile 状态、smoke resume execution、磁盘 tile source contract/assembly、受限 tiled iterator streaming writer、streaming writer pyramid contract、tile iterator streaming writer 原子发布事务 manifest、OME streaming writer progress sidecar、OME streaming started transaction temporary publish recovery、completed target OME-TIFF validation reuse、OME streaming disk-space preflight、production per-tile request manifest、production failed tile 显式 retry/resume、production tile backend execution evidence、generation output diagnostics manifest、smoke 四层 tile source streaming 写出（tile-streaming 路径不再预先构造整张 blended canvas，direct tile source 物化 manifest 可恢复）、torch-diffusion-smoke tile-streaming writer 接入、production-tile-stream 外部 tile generator backend + P4 writer tile-grid seam QC proxy + P4 stain/focus QC proxy + P4 mask-image tissue alignment QC proxy + P4 tile blending channel-wise 内存收敛 + P5 deterministic sampled style/texture policy artifact + fitted style latent prior + fitted texture morphology latent/codebook contract + prior production readiness contract gate + production prior component contract interface + sampled policy condition packet 条件摘要接入 + generation 输出摘要保留”阶段。
- 相比研究设计中的完整项目成果，已完成数据契约、基础 I/O、统计 prior、fitted RGB-stat style latent prior、fitted embedding-cluster texture morphology latent contract、prior production readiness contract gate、production prior component contract interface、deterministic sampled style/texture policy artifact、condition packet 对 sampled style/texture policy 的可审计消费、smoke/torch 输出对 sampled style/texture policy 摘要的可审计保留、checkpoint inference artifact contract gate、generation backend compatibility gate、inference architecture/condition contract gate、PyTorch diffusion smoke checkpoint inference planning contract、production training dataset contract、实际 training index JSONL 证据核对、training objective/loss/QC mapping contract、production training plan artifact、smoke 级生成、基础 OME-TIFF/QC/归档、writer tile-grid seam proxy、stain/focus QC proxy、mask 与图像组织区域一致性 proxy、UI 控制层、可交互 PySide6 配置页、GUI 内同步执行 queued job/刷新状态/查看输出摘要、CLI 命令分发拆分、PyTorch smoke training 纯 helper 拆分、可恢复 tile manifest contract、smoke tile resume execution、磁盘 `.npy` tile source contract 校验、磁盘 tile source 内存组装写出、受限 tile iterator streaming writer、tile iterator streaming writer 原子发布事务 manifest、OME streaming started transaction temporary publish recovery、smoke 四层直接 tile source streaming 写出、torch-diffusion-smoke tile-streaming writer 接入、production-tile-stream 外部 tile backend 磁盘级逐 tile 生成、per-tile request manifest 输入合同、failed tile 显式重试恢复和当前机器的 PyTorch/PySide6 依赖验证；尚未完成内置 production 级 latent diffusion/ControlNet 训练与推理、生产级 layout/style/texture prior、同一 OME-TIFF 文件内部中断追加写入。
- 按设计目标拆解，M1 基本完成；M2-M4/M6 为基础能力完成但仍偏 smoke/proxy 或外部 backend contract，其中 M6 新增可恢复 tile 状态、smoke resume execution、磁盘 tile source 发布前校验、内存组装写出、受限 tile iterator streaming、streaming writer pyramid contract、tile iterator streaming writer 原子发布事务 manifest、started transaction 完整临时 OME-TIFF 验证发布恢复、production per-tile request manifest、smoke 四层 tile source streaming 接入、tile-streaming 直接 tile source 写出、direct tile source 物化 resume manifest、torch-diffusion-smoke tile-streaming writer 接入、production-tile-stream 外部 tile generator backend、writer tile-grid seam QC proxy、stain/focus QC proxy 和 mask-image tissue alignment QC proxy；M5 仍为 smoke/proxy，但 checkpoint inference 可用性已从薄布尔声明收紧为文件/hash/inference/backend/architecture/condition gate，且 PyTorch diffusion smoke 训练产物现在可被 `torch-diffusion-smoke` planning 和 tile-streaming 输出链路消费；训练入口也新增 production training dataset contract、training index JSONL 证据核对、training objective/loss/QC mapping contract 和 production training plan artifact；M7 的本地同步 GUI flow 已补齐，后台 daemon、并发队列和运行中进程终止仍不属于当前实现。
- 当前 v0.72.32 作为本轮补救基线，尚未打 tag 或形成发布包；v0.72.32 是 P4 OME streaming writer progress evidence 增强，不新增内置 production 生成模型，也不实现同一 OME-TIFF 文件内部 append/resume。

## 已完成

| ID | 验收项 | 文件或测试证据 | 当前状态 | 备注 |
|---|---|---|---|---|
| AC-DOC-01 | 审计文件已迁移到 `docs/audit/` | `docs/audit/ACCEPTANCE_CHECKLIST.md`、`docs/audit/IMPLEMENTATION_PLAN.md`、`docs/audit/DECISIONS.md` | 已完成 | 原 `docs/` 根目录审计文件已移动 |
| AC-DOC-02 | 研究设计、开发附录、需求和变更日志存在 | `docs/plans/2026-05-18-he-wsi-generator-study-design.md`、`docs/dev/2026-05-23-he-wsi-generator-development-appendix.md`、`docs/DEMANDS.MD`、`docs/CHANGELOG.md` | 已完成 | 设计文档明确完整成果边界 |
| AC-CORE-01 | 版本号同步为 `v0.72.32` | `VERSION`、`pyproject.toml`、`src/he_wsi_generator/constants.py`、`configs/generation.default.json`、`tests/test_version.py` | 已完成 | v0.72.32 已同步版本文件和测试断言；本轮使用 `conda run -n MultiCenterWSIGenerator python -m unittest tests.test_outputs_qc_archive tests.test_generation_runner tests.test_schemas tests.test_cli tests.test_version -v` 通过，`Ran 84 tests in 3.295s OK`；收束审计补充执行 `conda run -n MultiCenterWSIGenerator python -m unittest discover -s tests -v` 通过，`Ran 300 tests in 28.868s OK`；CLI 版本输出 `v0.72.32`；关键文件 py_compile 通过，无输出；默认 generation config CLI 校验输出 `generation-config valid: configs/generation.default.json`；`git diff --check` 通过，无输出；尚未重新执行 editable install 或包元数据检查 |
| AC-CORE-02 | 包结构覆盖开发附录建议的核心模块 | `src/he_wsi_generator/{io,annotations,embeddings,priors,models,generation,qc,metadata,outputs,ui}` | 已完成 | core/CLI/UI 控制层边界已建立 |
| AC-CORE-03 | 基础 schema、manifest、label mapping、generation config、metadata、QC 校验可用 | `src/he_wsi_generator/schemas.py`、`tests/test_schemas.py`、`tests/test_cli.py` | 已完成 | 默认 generation config 本轮验证通过 |
| AC-CORE-04 | 统计 prior、condition packet、sampled layout mask 和 smoke generation 链路存在 | `src/he_wsi_generator/priors/`、`src/he_wsi_generator/generation/`、`tests/test_priors.py`、`tests/test_generation_conditioning.py`、`tests/test_generation_runner.py` | 已完成 | 当前为统计/smoke/proxy 能力，不是最终生成模型 |
| AC-CORE-05 | OME-TIFF 小型写出、mask 输出、QC、batch index、`qc_review` 文件级审阅工作流存在 | `src/he_wsi_generator/outputs/`、`src/he_wsi_generator/qc/`、`src/he_wsi_generator/metadata/archive.py`、`tests/test_outputs_qc_archive.py`、`tests/test_qc_review.py` | 已完成 | `qc_review` 已有独立构建和决策更新命令 |
| AC-CORE-06 | 历史真实 SVS 验证工件存在 | `build/validation/v0.62.0-291288/...` | 已完成 | 只能证明历史工件存在；不等于本轮重新跑完整链路 |
| AC-TEST-01 | 常规单元测试覆盖面较广 | 历史：`mamba run -n MultiCenterWSIGenerator python -m unittest discover -s tests -v`；当前：`conda run -n MultiCenterWSIGenerator python -m unittest discover -s tests -v` | 已完成 / 当前全量验证通过 | v0.72.14 全量单元测试通过，`Ran 287 tests in 22.283s OK`；v0.72.32 使用 conda 环境复跑 OME streaming writer progress evidence、outputs、generation、schema、CLI 和版本闭环通过，`Ran 84 tests in 3.295s OK`；收束审计补充执行全量单元测试通过，`Ran 300 tests in 28.868s OK`；尚未重跑 editable install 或包元数据 |
| AC-ENV-01 | 独立 conda 环境已创建并安装完整可选依赖 | `mamba create -y -n MultiCenterWSIGenerator -c conda-forge python=3.11 pip openslide`、`mamba run -n MultiCenterWSIGenerator python -m pip install -e '.[embeddings,outputs,training,torch,ui,yaml,wsi]'` | 已完成 | Python `3.11.15`；依赖包含 torch、PySide6、OpenSlide、YAML、tifffile、Pillow、numpy |
| AC-ENV-02 | 当前环境可运行 PyTorch smoke 训练/采样/生成测试 | `mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_torch_training -v`、`mamba run -n MultiCenterWSIGenerator python -c 'import torch'` | 已完成 | PyTorch `2.12.0+cu130`，CUDA 可用，GPU 为 `NVIDIA GeForce RTX 5060 Ti` |
| AC-ENV-03 | 当前环境可导入 PySide6 并创建现有 UI 窗口 | `QT_QPA_PLATFORM=offscreen mamba run -n MultiCenterWSIGenerator python - <<'PY' ... create_main_window() ... PY` | 已完成 | offscreen smoke 返回窗口标题 `MultiCenterWSIGenerator`、widget count `86`、button count `5`，并确认执行/刷新/摘要按钮存在 |
| AC-P1-01 | 仓库内实体 `AGENTS.md` 已持久化 | `AGENTS.md` | 已完成 | 记录本仓库通用、代码、文档、版本和 worker 编排规则 |
| AC-P1-02 | `qc_review` 接入通用 `validate` CLI/schema kind | `src/he_wsi_generator/schemas.py`、`src/he_wsi_generator/cli.py`、`tests/test_qc_review.py` | 已完成 | `he-wsi-gen validate qc-review <path>` 可校验有效 artifact，非法 `artifact_type` 显式失败 |
| AC-P1-03 | 输出摘要严格遵循 metadata 契约 | `src/he_wsi_generator/ui/controller.py`、`tests/test_ui.py` | 已完成 | `collect_output_summary()` 校验 metadata schema，并校验 metadata/QC/review `generated_id` 一致性 |
| AC-P1-04 | README 里程碑状态区分骨架/控制层与完整成果 | `README.md` | 已完成 | M5/M7 和当前边界已明确 production 模型与真实 PySide6 配置页未完成 |
| AC-P2-01 | 真正可交互的 PySide6 自定义配置页 | `src/he_wsi_generator/ui/pyside_app.py`、`tests/test_ui.py` | 已完成 | UI 表单支持配置保存路径、prior/checkpoint/output/generated id、backend、anchor preset、structure anchor、seed、sample steps、overlap、condition packet、training index、QC non-copy 和 6 类 label mapping |
| AC-P2-02 | UI 表单保存 generation config 前执行显式校验 | `src/he_wsi_generator/ui/workflow.py`、`tests/test_ui_workflow.py`、`tests/test_ui.py` | 已完成 | 非法 label mapping、缺必填字段、source-anchored 缺 source WSI、torch backend 缺 training index 均显式失败 |
| AC-P2-03 | UI 可创建 queued local generation job | `src/he_wsi_generator/ui/pyside_app.py`、`src/he_wsi_generator/ui/workflow.py`、`tests/test_ui.py`、`tests/test_ui_workflow.py` | 已完成 | 创建 `<output-root>/ui_jobs/<generated-id>/job.json`，不自动执行生成 |
| AC-P2-04 | GUI 内同步执行、刷新 job 状态和查看输出摘要 | `src/he_wsi_generator/ui/pyside_app.py`、`src/he_wsi_generator/ui/workflow.py`、`tests/test_ui.py`、`tests/test_ui_workflow.py` | 已完成 | UI 调用 workflow helper 执行 queued job、刷新 job record，并在 completed 后读取 metadata/QC/可选 qc_review 摘要；不包含后台 daemon 或运行中取消 |
| AC-P4-01 | 可恢复 tile manifest contract | `src/he_wsi_generator/generation/tiling.py`、`tests/test_generation_tiling.py`、`.agent/reports/p4-resumable-tile-manifest-20260524.md` | 已完成 | 支持从 traversal plan 构建状态 manifest、标记 completed/failed、计算 resume/next tile，pending/failed/非 row-major 完成或计数不一致显式失败 |
| AC-P4-02 | 磁盘 `.npy` tile source contract 校验 | `src/he_wsi_generator/outputs/ome_tiff.py`、`tests/test_outputs_qc_archive.py`、`.agent/reports/p4-ome-tiff-streaming-contract-20260524.md` | 已完成 | 写出前校验 expected count、level/tile index、路径存在、shape、dtype 和 completed 状态；pending/failed/missing/duplicate 等显式失败；返回 `partial_contract_only` |
| AC-P4-03 | smoke tile resume execution | `src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/cli.py`、`src/he_wsi_generator/cli_commands.py`、`tests/test_generation_runner.py`、`.agent/reports/p4-generation-resume-execution-20260524.md` | 已完成 | `smoke-cascade` 写出 tile `.npy`、`tile_manifest.json` 和 `tile_source_manifest.json`；`--resume-tile-manifest` 可继续 pending tile；failed/gapped/missing completed tile 显式失败 |
| AC-P4-04 | 磁盘 tile source 内存组装写出 | `src/he_wsi_generator/outputs/ome_tiff.py`、`src/he_wsi_generator/outputs/__init__.py`、`tests/test_outputs_qc_archive.py`、`.agent/reports/p4-disk-tile-assembly-writer-20260524.md` | 已完成 | `write_pyramid_ome_tiff_from_tile_sources()` 校验 level shape、tile origin、write region 与 coverage 后组装写出；明确 `production_streaming=false` |
| AC-P4-05 | 磁盘 tile source tiled iterator streaming 写出 | `src/he_wsi_generator/outputs/ome_tiff.py`、`src/he_wsi_generator/outputs/__init__.py`、`tests/test_outputs_qc_archive.py`、`.agent/reports/p4-tile-iterator-writer-20260524.md` | 已完成 | `write_pyramid_ome_tiff_streaming_from_tile_sources()` 按 TIFF tile grid 从磁盘逐 tile 写出，不组装完整 level array；报告 `production_streaming=true`、`resume_capable=false` |
| AC-P4-06 | smoke-cascade 显式接入 tile streaming writer | `src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/cli.py`、`src/he_wsi_generator/cli_commands.py`、`tests/test_generation_runner.py`、`.agent/reports/p4-smoke-streaming-writer-integration-20260524.md` | 已完成 | `run-generation --backend smoke-cascade --wsi-writer tile-streaming` 使用 tile iterator writer；默认 `array` 路径不变；`--resume-tile-manifest` 仍只支持 smoke-cascade |
| AC-P4-07 | streaming writer pyramid level 契约加固 | `src/he_wsi_generator/outputs/ome_tiff.py`、`tests/test_outputs_qc_archive.py`、`.agent/reports/p4-streaming-writer-pyramid-contract-20260524.md` | 已完成 | manifest levels 必须按 high-to-low resolution 声明；缺失 level shape 或后续 level 尺寸反增显式失败；报告记录 `pyramid_order`、`level_order` 和 `pyramid_position` |
| AC-P4-08 | smoke-cascade tile-streaming 四层 OME-TIFF 写出 | `src/he_wsi_generator/generation/executor.py`、`tests/test_generation_runner.py`、`.agent/reports/p4-smoke-multilevel-tile-source-20260524.md` | 已完成 | 显式 `--wsi-writer tile-streaming` 会物化四层 `tile_source_manifest.streaming.json` 与 `streaming_tiles/*.npy`，并写出四层 OME-TIFF；v0.72.18 后该路径不再基于已在内存中生成的 smoke canvas |
| AC-P4-09 | smoke-cascade tile-streaming 顺序物化四层 pyramid levels | `src/he_wsi_generator/generation/executor.py`、`tests/test_generation_runner.py`、`.agent/reports/p4-smoke-streaming-sequential-materialization-20260524.md` | 已完成 / 已被 v0.72.18 收敛 | 历史 v0.72.14 路径按 high-to-low pyramid 顺序逐层物化 smoke levels；v0.72.18 改为按 writer tile grid 直接生成四层 tile source，默认 `array` writer 行为不变 |
| AC-P4-10 | tile iterator streaming writer 原子发布事务 manifest | `src/he_wsi_generator/outputs/ome_tiff.py`、`tests/test_outputs_qc_archive.py`、`.agent/reports/p4-streaming-publish-transaction-20260525.md` | 已完成 | `write_pyramid_ome_tiff_streaming_from_tile_sources()` 先写同目录临时 OME-TIFF，校验 OME/pyramid shape 后原子替换目标路径；`<target>.transaction.json` 记录 started/completed/failed、目标/临时路径、tile source manifest 摘要、失败原因、`atomic_publish=true` 和 `resume_capable=false`；失败时清理临时文件并保留既有目标 |
| AC-P4-19 | OME streaming started transaction temporary publish recovery | `src/he_wsi_generator/outputs/ome_tiff.py`、`tests/test_outputs_qc_archive.py` | 已完成 | `write_pyramid_ome_tiff_streaming_from_tile_sources()` 可在上次 `started` transaction 留下完整临时 OME-TIFF 时，校验目标路径、tile source manifest 路径和 pyramid shapes 后直接发布该临时文件；completed transaction 记录 `recovery_action=published_existing_temporary_ome_tiff`，报告记录 `recovered_from_temporary=true`；`conda run -n MultiCenterWSIGenerator python -m unittest tests.test_outputs_qc_archive tests.test_generation_runner tests.test_schemas tests.test_cli tests.test_version -v` 通过，`Ran 80 tests in 2.615s OK`；仍不是同一 OME-TIFF 文件内部中断追加写入，`resume_capable=false` 保留 |
| AC-P4-12 | generation output diagnostics manifest contract | `src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/schemas.py`、`src/he_wsi_generator/cli.py`、`tests/test_generation_runner.py`、`tests/test_schemas.py`、`tests/test_cli.py`、`tests/test_torch_training.py`、`.agent/reports/p4-generation-output-diagnostics-contract-20260525.md` | 已完成 | `run_smoke_generation()` 与 `run_torch_diffusion_smoke_generation()` 写出 `generation_output_diagnostics.json`；metadata、generation run summary 和返回值引用该路径；smoke 汇总 tile execution/source 状态，tile-streaming 记录 transaction manifest 与 `resume_capable=false`，torch-smoke 显式 `tile_execution.applicable=false`；validator/CLI 可独立校验 |
| AC-P4-13 | smoke tile-streaming direct tile source generation | `src/he_wsi_generator/generation/executor.py`、`tests/test_generation_runner.py` | 已完成 | `run_smoke_generation(..., wsi_writer="tile-streaming")` 直接按 pyramid level 和 TIFF tile grid 写出四层 `.npy` tile source manifest，不再调用 `blend_rgb_tiles()` 构造整张 blended canvas；`tests.test_generation_runner` 通过，`Ran 23 tests in 1.263s OK`；仍不是 production diffusion backend 或可恢复 OME-TIFF 续写 |
| AC-P4-14 | smoke direct tile source materialization resume manifest | `src/he_wsi_generator/generation/executor.py`、`tests/test_generation_runner.py` | 已完成 | `_write_smoke_direct_multilevel_tile_source_manifest()` 维护 `generation_status` 与 completed/pending/failed 计数；重跑时校验并复用已完成 streaming tile，补齐 pending tile；`test_run_smoke_generation_tile_streaming_resumes_partial_tile_source_manifest` 通过；仍不是 OME-TIFF 文件级续写 |
| AC-P4-15 | torch-diffusion-smoke tile-streaming writer 接入 | `src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/cli.py`、`src/he_wsi_generator/cli_commands.py`、`tests/test_torch_training.py`、`tests/test_generation_runner.py` | 已完成 | `run_torch_diffusion_smoke_generation(..., wsi_writer="tile-streaming")` 会将四层 sample preview 物化为 `tile_source_manifest.streaming.json` 与 `.npy` tile source，并调用 tiled iterator writer 写出 OME-TIFF；CLI 不再因 `--wsi-writer tile-streaming` 拒绝 torch backend；`mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_torch_training tests.test_generation_runner tests.test_version -v` 通过，`Ran 53 tests in 15.011s OK`；仍不是 production inference backend 或可恢复 OME-TIFF 续写 |
| AC-P4-11 | mask-image tissue alignment QC proxy | `src/he_wsi_generator/qc/engine.py`、`tests/test_outputs_qc_archive.py`、`.agent/reports/p4-mask-image-tissue-alignment-qc-20260525.md` | 已完成 | `build_qc_report()` 现在记录 `mask_image_tissue_alignment_proxy`，用生成图像组织区域 proxy 与 `mask > 0` 区域一致性暴露粗粒度错位；明显错位会让 mask-region 与 overall QC fail；该指标不是专家级语义分类器 |
| AC-P4-16 | writer tile-grid seam QC proxy | `src/he_wsi_generator/qc/engine.py`、`tests/test_outputs_qc_archive.py` | 已完成 | `build_qc_report()` 的 `seam_score_proxy` 优先读取 writer chunk/tile shape，检查所有内部 tile 边界；缺少 writer grid 时回退中线 proxy；明显非中线 tile seam 会 fail；`mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_outputs_qc_archive tests.test_version -v` 通过，`Ran 35 tests in 0.098s OK`；仍不是专家级 seam detector |
| AC-P4-17 | stain/focus QC proxy | `src/he_wsi_generator/qc/engine.py`、`tests/test_outputs_qc_archive.py` | 已完成 | `build_qc_report()` 现在记录 `stain_color_separation_proxy` 与 `focus_edge_density_proxy`；近单色 RGB 通道和几乎无局部边缘对比会让 WSI/tile/overall QC fail；`mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_outputs_qc_archive tests.test_version -v` 通过，`Ran 36 tests in 0.105s OK`；仍不是专家级 stain/focus 质量模型 |
| AC-P4-18 | production-tile-stream 外部 tile backend 接入 | `src/he_wsi_generator/generation/production_streaming.py`、`src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/cli.py`、`src/he_wsi_generator/cli_commands.py`、`src/he_wsi_generator/schemas.py`、`tests/test_generation_runner.py` | 已完成 | `run-generation --backend production-tile-stream --wsi-writer tile-streaming` 要求 production-ready checkpoint 和 `external_tile_generator_v1` artifact；按四层 pyramid tile grid 逐 tile 写出磁盘 RGB tile、level0 mask tile、可恢复 `production_tile_source_manifest.json`、memmap mask、OME-TIFF、streaming QC、metadata、batch index 和 diagnostics；v0.72.24 集中验证通过 `tests.test_generation_runner tests.test_schemas tests.test_cli tests.test_version`，`Ran 45 tests in 2.807s OK`；v0.72.26 相关 writer/generation/schema/CLI/version 集中验证通过，`Ran 80 tests in 2.615s OK`；仍不实现内置 production diffusion 模型或同一 OME-TIFF 文件内部中断追加写入 |
| AC-P4-20 | production tile request manifest contract | `src/he_wsi_generator/generation/production_streaming.py`、`tests/test_generation_runner.py`、`docs/DEMANDS.MD` | 已完成 | v0.72.27 为 `production-tile-stream` 增加 `request_manifest_type=production_tile_request_v1`、每 tile 稳定 `tile_request_path` 和 `production_tile_requests/*.request.json`；外部 backend 命令支持 `{tile_request_path}`，request JSON 记录 tile、输出路径、prior、checkpoint、backend 和 condition packet 摘要；resume 校验将 `tile_request_path` 作为不可变字段；`conda run -n MultiCenterWSIGenerator python -m unittest tests.test_generation_runner tests.test_schemas tests.test_cli tests.test_version -v` 通过，`Ran 46 tests in 2.730s OK`；仍不是内置 production diffusion 模型 |
| AC-P4-21 | production failed tile retry/resume | `src/he_wsi_generator/generation/production_streaming.py`、`src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/cli.py`、`src/he_wsi_generator/cli_commands.py`、`tests/test_generation_runner.py` | 已完成 | v0.72.28 为 `production-tile-stream` 增加显式 `retry_failed_tiles` / `--retry-failed-tiles`；默认仍拒绝 failed tile manifest，显式 retry 时先校验 immutable manifest 与 `tile_request_path`，再把 failed record 重置为 pending 并重试外部 backend；完成后保留 `retry_from_failed`、`previous_status`、`previous_error_message`、`retry_count` 和 attempt count 审计字段；`conda run -n MultiCenterWSIGenerator python -m unittest tests.test_generation_runner tests.test_schemas tests.test_cli tests.test_version -v` 通过，`Ran 47 tests in 3.778s OK`；仍不是 OME-TIFF 文件内部续写或内置 production diffusion 模型 |
| AC-P4-22 | OME streaming completed target validation reuse | `src/he_wsi_generator/outputs/ome_tiff.py`、`src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/schemas.py`、`tests/test_outputs_qc_archive.py`、`tests/test_generation_runner.py` | 已完成 | v0.72.29 为 tile iterator streaming writer 增加 completed transaction 目标复用；当 transaction、target path、tile source manifest path 和目标 OME pyramid shapes 与当前 plan 匹配时，跳过 tile iterator 重写，记录 `recovery_action=validated_existing_target_ome_tiff`、`reused_existing_target=true`；`conda run -n MultiCenterWSIGenerator python -m unittest tests.test_outputs_qc_archive tests.test_generation_runner tests.test_schemas tests.test_cli tests.test_version -v` 通过，`Ran 83 tests in 3.420s OK`；关键文件 py_compile、默认 generation config CLI 校验和 `git diff --check` 均通过；仍不是 OME-TIFF 文件内部 partial tile 续写 |
| AC-P4-23 | OME streaming disk-space preflight | `src/he_wsi_generator/outputs/ome_tiff.py`、`src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/schemas.py`、`tests/test_outputs_qc_archive.py`、`tests/test_generation_runner.py`、`tests/test_schemas.py` | 已完成 | v0.72.30 为 tile iterator streaming writer 增加写入前磁盘空间 preflight；正常完整写出前按 raw pyramid byte estimate 加同等安全余量检查目标文件系统，空间不足时在 tile iterator 开始前显式失败并写出 failed transaction；diagnostics `writer_summary.disk_space_preflight` 保留 preflight 摘要并由 schema 校验；`conda run -n MultiCenterWSIGenerator python -m unittest tests.test_outputs_qc_archive tests.test_generation_runner tests.test_schemas tests.test_cli tests.test_version -v` 通过，`Ran 84 tests in 3.753s OK`；仍不是精确 TIFF 大小预测或 OME-TIFF 文件内部 partial tile 续写 |
| AC-P4-24 | production tile backend execution evidence | `src/he_wsi_generator/generation/production_streaming.py`、`src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/schemas.py`、`tests/test_generation_runner.py` | 已完成 | v0.72.31 为 `production-tile-stream` 新完成 tile record 增加 request、backend execution 和 RGB/mask output evidence；manifest 顶层记录 `backend_execution_summary`，diagnostics `tile_source.backend_execution_summary` 保留覆盖统计并由 schema 校验；开发过程中 RED 失败符合预期，旧实现缺 `backend_execution_summary`；`conda run -n MultiCenterWSIGenerator python -m unittest tests.test_generation_runner tests.test_schemas tests.test_cli tests.test_version -v` 通过，`Ran 47 tests in 3.578s OK`；默认 generation config CLI 校验和 `git diff --check` 通过；仍不是内置 production diffusion 模型或 OME-TIFF 文件内部 partial tile 续写 |
| AC-P4-25 | OME streaming writer progress evidence | `src/he_wsi_generator/outputs/ome_tiff.py`、`src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/schemas.py`、`tests/test_outputs_qc_archive.py`、`tests/test_generation_runner.py`、`tests/test_schemas.py` | 已完成 | v0.72.32 为 tile iterator streaming writer 新增 `<target>.progress.json`，记录 planned/yielded/completed tile 数、当前 level/tile grid、last tile、失败原因和 `progress_semantics=tiles_yielded_to_tifffile_iterator_not_ome_internal_resume`；transaction manifest 与 diagnostics `writer_summary.progress_summary` 保留进度摘要；开发过程中 RED 失败符合预期，旧实现缺 `progress_manifest_path`；`conda run -n MultiCenterWSIGenerator python -m unittest tests.test_outputs_qc_archive tests.test_generation_runner tests.test_schemas tests.test_cli tests.test_version -v` 通过，`Ran 84 tests in 3.295s OK`；仍不是 OME-TIFF 文件内部 partial tile 续写 |
| AC-P3-01 | checkpoint inference artifact / backend / architecture / condition contract gate | `src/he_wsi_generator/models/training.py`、`src/he_wsi_generator/generation/planner.py`、`tests/test_models_generation.py`、`tests/test_generation_runner.py`、`.agent/reports/p3-checkpoint-inference-contract-20260525.md`、`.agent/reports/p3-backend-compatibility-contract-20260525.md`、`.agent/reports/p3-inference-architecture-condition-contract-20260525.md` | 已完成 | `usable_for_inference=true` 现在要求 trained 状态、backend/target/checkpoint/hash 字段、真实 checkpoint 文件、matching SHA-256、显式 `inference_contract`、非空 `compatible_generation_backends`、`model_architecture_contract` 和 `condition_input_contract`；generation plan 会校验当前 backend 是否被 checkpoint 允许消费，并保留模型架构/条件输入摘要；薄 JSON、缺文件、hash mismatch、缺 contract、缺 backend compatibility、backend 不匹配、缺模型架构、缺必需条件输入或 status/usable 矛盾会显式失败；`tests.test_models_generation tests.test_generation_runner tests.test_version` 通过，`Ran 59 tests in 1.485s OK`；非 production skeleton checkpoint 仍保持 `usable_for_inference=false` |
| AC-P3-04 | PyTorch diffusion smoke checkpoint inference planning contract | `src/he_wsi_generator/models/torch_training_contracts.py`、`src/he_wsi_generator/generation/planner.py`、`src/he_wsi_generator/generation/executor.py`、`tests/test_torch_training.py`、`.agent/reports/p3-torch-diffusion-smoke-inference-planning-20260525.md` | 已完成 | `train_torch_diffusion_smoke_model()` 产物现在写入 `usable_for_inference=true` 与 `inference_contract`，仅允许 `torch-diffusion-smoke` backend 消费；`create_generation_plan(..., generation_backend="torch-diffusion-smoke")` 可保留 backend/architecture/condition 摘要，同一 checkpoint 拒绝 `smoke-cascade`；该 checkpoint 仍 `production_ready=false`，不是 production latent diffusion / ControlNet / DiT；定向闭环测试通过，`Ran 3 tests in 2.084s OK` |
| AC-P3-02 | production training dataset contract 与 training index JSONL 证据核对 | `src/he_wsi_generator/models/training.py`、`src/he_wsi_generator/models/training_index.py`、`tests/test_models_generation.py`、`tests/test_training_index.py`、`.agent/reports/p3-production-training-contract-20260525.md` | 已完成 | `init-training-run` 要求 `training_backend=latent_diffusion_unet`，并校验 `dataset_contract` 的 training index path、production readiness、样本数、train split、四层 cascade 记录数、必需条件输入和 6 类 mask schema；v0.72.7 进一步读取实际 training index JSONL，核对 record 数、split/level 计数、tile 坐标、conditioning 字段和 mask class mapping；`build_training_index()` 同步写入 `texture_token_source` 证据；run manifest 与 skeleton checkpoint 记录通过校验的契约，但 checkpoint 仍为 `not_trained` / `usable_for_inference=false` |
| AC-P3-03 | training objective/loss/QC mapping contract gate | `src/he_wsi_generator/models/training.py`、`tests/test_models_generation.py`、`.agent/reports/p3-training-objective-contract-20260525.md` | 已完成 | `init-training-run` 要求 `training_objective_contract` 声明五类训练约束、非负 loss weights、`image_generator` / `wsi_consistency` 阶段目标映射和 QC 指标映射；run manifest 与 skeleton checkpoint 记录通过校验的契约，但仍不执行真实 loss 计算或 production training loop |
| AC-P3-05 | production training plan artifact | `src/he_wsi_generator/models/training.py`、`tests/test_models_generation.py` | 已完成 | `init-training-run` 现在写出 `training_plan.json`，记录 `prior_ready -> image_generator -> wsi_consistency` 三阶段、stage objectives、condition inputs、outputs 和 QC mapping；`training_run.json` 与 skeleton checkpoint 都引用 `training_plan_path`；`mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_models_generation tests.test_version -v` 通过，`Ran 36 tests in 0.100s OK`；仍不执行真实 production diffusion loss 优化或生成 inference-ready 权重 |
| AC-DEV-05 | P4 tile blending channel-wise 内存收敛 | `src/he_wsi_generator/generation/tiling.py`、`tests/test_generation_tiling.py`、`.agent/reports/p4-tile-blend-memory-reduction-20260524.md` | 已完成 | `blend_rgb_tiles()` 改为按 channel-by-channel 累积中间结果，避免整块 RGB tile 先做 float64 cast；现有 overlap blending 和 uncovered canvas 失败行为保持不变 |
| AC-P5-01 | deterministic sampled style policy artifact | `src/he_wsi_generator/priors/style.py`、`src/he_wsi_generator/cli.py`、`src/he_wsi_generator/cli_commands.py`、`tests/test_style_prior.py`、`.agent/reports/p5-style-sampling-policy-20260524.md` | 已完成 | `sample_style_policy_from_prior()` 与 `sample-style-policy` 可从 style prior 按 seed 选择 tile-level style record，并在 v0.72.22 后保留 `style_latent` 与 encoder reference；仍不是深度 trainable style encoder |
| AC-P5-02 | deterministic sampled texture policy artifact | `src/he_wsi_generator/priors/texture.py`、`src/he_wsi_generator/cli.py`、`src/he_wsi_generator/cli_commands.py`、`tests/test_texture_prior.py`、`.agent/reports/p5-texture-sampling-policy-20260524.md` | 已完成 | `sample_texture_policy_from_prior()` 与 `sample-texture-policy` 可从统计型 texture prior 按 seed 选择 prototype；仍不是 trainable texture codebook |
| AC-P5-03 | sampled style/texture policy condition packet 条件摘要接入 | `src/he_wsi_generator/generation/conditioning.py`、`src/he_wsi_generator/cli.py`、`src/he_wsi_generator/cli_commands.py`、`tests/test_generation_conditioning.py`、`.agent/reports/p5-condition-sampled-policy-20260524.md` | 已完成 | `build-condition-packet` 可选读取 `--sampled-style-policy` / `--sampled-texture-policy`，校验 source prior path 与关键字段，并写入 `artifact_inputs` 与 `conditions.style_seed` / `conditions.texture_token`；仍不自动调用 sampler 或改变 generation backend |
| AC-P5-04 | sampled style/texture policy generation 输出摘要保留 | `src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/models/torch_training.py`、`tests/test_generation_runner.py`、`tests/test_torch_training.py`、`.agent/reports/p5-generation-sampled-policy-summary-20260524.md` | 已完成 | smoke metadata/run summary 与 torch sample manifest/generation summary 会保留 selected style/token 摘要；缺关键字段显式失败；仍不改变 condition feature vector 或生成 backend |
| AC-P5-05 | prior production readiness contract gate | `src/he_wsi_generator/priors/artifacts.py`、`tests/test_priors.py`、`.agent/reports/p5-prior-production-readiness-contract-20260525.md` | 已完成 | `build_prior_manifest_from_artifacts()` 写入 `production_readiness`，标记统计型 layout/style/texture prior 为非 production-ready；`validate_prior_manifest()` 拒绝缺 component contract、组件未 production-ready 或 statistical/proxy backend 却声明 `production_ready=true` 的 manifest；该能力不是 production trainable prior |
| AC-P5-06 | production prior component contract interface | `src/he_wsi_generator/priors/artifacts.py`、`tests/test_priors.py` | 已完成 | `production_ready=true` 现在要求 component `contract_version=production_prior_component_v1`、layout/style/texture 所需 `condition_outputs`，以及与 manifest artifact path/hash 匹配的 `training_evidence`；`mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_priors tests.test_version -v` 通过，`Ran 14 tests in 0.090s OK`；仍不是 production trainable prior |
| AC-P5-07 | fitted style latent prior | `src/he_wsi_generator/priors/style.py`、`tests/test_style_prior.py` | 已完成 | `build_style_prior_from_training_index()` 写出 `fitted_rgb_stats_pca_v1` encoder、feature schema、components、explained variance 和 tile-level `style_latent`；`sample_style_policy_from_prior()` 输出 selected `style_latent` 和 encoder reference；`mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_style_prior tests.test_version -v` 通过，`Ran 8 tests in 0.413s OK`；仍不是深度 trainable style encoder 或 VAE style latent |
| AC-P5-08 | fitted texture morphology latent/codebook contract | `src/he_wsi_generator/priors/texture.py`、`src/he_wsi_generator/generation/conditioning.py`、`src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/models/torch_training.py`、`tests/test_texture_prior.py`、`tests/test_generation_conditioning.py`、`tests/test_generation_runner.py`、`tests/test_torch_training.py` | 已完成 | `build_texture_prior_from_embedding_cache()` 写出 `fitted_embedding_cluster_codebook_v1`、prototype `texture_token` 和 `morphology_latent`；`sample_texture_policy_from_prior()` 输出 selected token / morphology latent / codebook reference；condition packet、smoke summary 和 torch smoke summary 保留该条件对象；`mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_texture_prior tests.test_generation_conditioning tests.test_generation_runner tests.test_torch_training tests.test_version -v` 通过，`Ran 69 tests in 20.023s OK`；仍不是 trainable texture codebook、VQ-VAE 或 production morphology token sampler |
| AC-AUDIT-01 | 本轮复审计已捕获当前工作区状态 | `git status --short`、`docs/CHANGELOG.md`、`docs/dev/2026-05-23-he-wsi-generator-development-appendix.md`、`docs/plans/2026-05-18-he-wsi-generator-study-design.md`、`git diff --check` | 部分完成 | v0.72.32 作为本轮主分支补救基线；尚未 tag 或打包发布；功能开发阶段未启动 worker，本次收束阶段使用两个只读 multiagent explorer 审查源码/测试/版本和文档/审计/report 一致性；按当前 `AGENTS.md` 约束，已在代码集中验证通过后维护 OME streaming writer progress evidence 相关设计/开发文档最小章节代码追踪块；本轮 changelog 顶部记录 conda 验证结果 |

## 缺失

| ID | 验收项 | 文件或测试证据 | 当前状态 | 备注 |
|---|---|---|---|---|
| AC-MISS-04 | Production 级结构锚定多分辨率 latent diffusion / ControlNet / DiT 训练和推理 | `src/he_wsi_generator/models/training.py`、`src/he_wsi_generator/models/torch_training.py`、`README.md` 当前边界说明 | 部分完成 / 仍缺失内置 production 模型 | v0.72.32 保留 production-tile-stream 外部 tile backend 接入合同、per-tile request manifest 输入合同、failed tile 显式重试恢复、completed target OME 验证复用、OME streaming 磁盘空间 preflight、backend execution evidence 和 writer progress evidence；但仍没有内置 production latent diffusion / ControlNet / DiT 训练 loop、本体推理模型或真实训练产出的 production checkpoint |
| AC-MISS-05 | 生产级 layout/style/texture prior 和 style sampling policy | `src/he_wsi_generator/priors/`、`src/he_wsi_generator/generation/conditioning.py`、`src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/models/torch_training.py` | 部分完成 / 仍缺失 production prior | v0.72.23 已继承 deterministic sampled style/texture policy artifact、condition packet 审计消费、generation 输出摘要保留、prior production readiness contract gate 和 production prior component contract interface，并新增 fitted RGB-stat style latent prior、fitted embedding-cluster texture codebook 和 morphology latent contract；当前仍未实现深度 production trainable style encoder、trainable texture codebook、VQ-VAE 或 production morphology token sampler |
| AC-MISS-06 | 生产级 gigapixel OME-TIFF 流式写出、恢复和磁盘级 tile streaming | `src/he_wsi_generator/outputs/ome_tiff.py`、`src/he_wsi_generator/generation/tiling.py`、`src/he_wsi_generator/generation/executor.py`、`src/he_wsi_generator/generation/production_streaming.py`、`README.md` 当前边界说明 | 部分完成 / 同一 OME-TIFF 内部续写仍缺失 | v0.72.32 在 production-tile-stream 外部 tile backend、磁盘级逐 tile 生成、per-tile request manifest、backend execution evidence、failed tile 显式重试恢复、可恢复 tile source manifest、memmap mask、streaming QC、tiled iterator 原子发布、progress sidecar、started 临时 OME 发布恢复、completed target OME 验证复用和写入前磁盘空间 preflight 基础上继续推进恢复可靠性；仍没有同一 OME-TIFF 文件内部中断追加写入或内置 production diffusion 模型 |

## 偏离

| ID | 验收项 | 文件或测试证据 | 当前状态 | 备注 |
|---|---|---|---|---|
| AC-DEV-03 | CLI 与 torch smoke 训练代码保持低维护风险 | `src/he_wsi_generator/cli.py` 662 行、`src/he_wsi_generator/cli_commands.py` 505 行、`src/he_wsi_generator/models/torch_training.py` 1445 行、`src/he_wsi_generator/models/torch_training_contracts.py` 495 行 | 已完成 | 行为保持拆分完成，CLI 命令分发与纯 helper 已从主大文件中迁出，后续维护风险已明显下降 |
| AC-DEV-04 | P4 输出可靠性仍弱于完整设计 | `src/he_wsi_generator/outputs/ome_tiff.py`、`README.md` 当前边界说明 | 偏离 | 当前完成可恢复状态、smoke resume、发布前 contract gate、内存组装写出、受限 tile iterator streaming、streaming writer 原子发布事务、progress sidecar、smoke 四层直接 tile source streaming 接入和 direct tile source 物化恢复；OME-TIFF 文件本身仍不可中断续写 |

## 未验证

| ID | 验收项 | 文件或测试证据 | 当前状态 | 备注 |
|---|---|---|---|---|
| AC-VER-03 | 真实 SVS v0.62.0 全链路生成复跑证据 | `build/validation/v0.72.1-291288/...`、`/home/muhengliao/LMH2025/Data/raw_data/Pancancer_Fanhong/Breast_cancer_N=137/291288_.svs` | 历史已完成 / 本次未重跑 | 真实 SVS smoke/proxy 复跑证据来自历史 v0.72.1 轮次；本次 v0.72.32 收束未重新执行真实 SVS 全链路 |
| AC-VER-04 | 完整 production 项目成果可验收 | 研究设计 Phase 4/5/6、README 当前边界 | 未验证 | 关键 production 模型和完整 GUI 尚不存在，不能验收 |
| AC-VER-05 | v0.72.32 tag/发布包状态 | `git tag --list` 未在本轮执行发布流程 | 未验证 | v0.72.32 尚未 tag 或打包发布 |
| AC-VER-06 | worker 隔离区清理状态 | `git worktree list --porcelain` | 未验证 | 历史 worker worktree 与本轮 worker worktree 仍存在；当前作为审计证据保留，未清理 |
