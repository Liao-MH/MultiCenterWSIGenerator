# Repository Working Rules

本文件固化本仓库内 Codex 的常用工作规则。若用户在当前会话中给出更新或更具体的指令，以用户最新指令为准。
如有需要，"/home/muhengliao/LMH2025/Data/raw_data/Pancancer_Fanhong/Breast_cancer_N=137/291288_.svs"可作为测试样本

## 通用原则

1. 先读后写，修改前必须理解现有文件和上下文。
2. 输出保持简洁，但推理和验证要严谨。
3. 优先小范围编辑，不整篇重写无关文件。
4. 不重复读取已经确认且未变化的文件。
5. 默认跳过超过 100KB 的文件，除非任务明确需要；大型文档只读相关片段。
6. 会话变长时建议用户运行 `/cost` 关注缓存比例。
7. 切换到无关任务时建议开启新会话。
8. 宣称完成前必须测试或明确说明无法测试的原因。
9. 不用奉承式开场或收尾。
10. 保持方案简单直接。

## 代码实现原则

1. 当前需求优先，避免为假设性的未来需求做过度设计。
2. 只处理与当前需求强相关、且高概率出现的边界和兼容场景。
3. 对明确非法、缺失或不符合任务要求的输入显式报错，不使用 silent fallback 掩盖问题。
4. 最小改动，除非任务需要，不做无关重构。
5. 只在关键分支、兼容处理、失败条件和重要假设处写有价值注释。

## 测试节奏规则

1. 本项目采用“开发文档中代码里程碑完成后集中验证”的测试节奏。
2. 开发过程中应优先完成当前代码里程碑对应的功能闭环，不要在实现每个内部 helper、中间函数或临时分支时立即追加大量细碎测试。
3. 每完成一个开发文档中的代码里程碑后，再集中补充和运行测试，包括 smoke test。
4. 测试优先覆盖：
   - 核心成功路径；
   - 关键数据契约；
   - 高影响边界条件；
   - 高概率用户错误；
   - 已知回归问题。
5. 避免为以下内容设计冗余测试：
   - 低概率非法输入；
   - 内部 helper 的重复防御逻辑；
   - 完整错误文案逐字匹配；
   - 不影响业务契约的临时实现细节。
6. 只有在以下情况中，才应在里程碑完成前追加针对性测试或快速验证：
   - 该 helper 承担关键业务规则；
   - 该 helper 被多个核心流程复用；
   - 本次改动涉及跨模块数据契约；
   - 当前问题来自已知回归；
   - 不提前验证会显著增加后续定位成本。
7. smoke test 应作为里程碑完成后的集中验证内容，不应在每个小函数完成后反复运行，除非当前变更直接影响启动、入口命令、主流程编排或关键 I/O。
8. 最终回复必须说明：
   - 本次完成的是哪个代码里程碑；
   - 执行了哪些测试；
   - 哪些测试暂未执行以及原因。

## 代码追踪块补充要求
1. 当前长期开发目标要求每次实现、修改或重构功能后，立即维护设计文档和开发文档中相关最小级标题下的章节级代码追踪块；该要求优先于此前“所有里程碑结束后统一补充”的旧节奏。
2. 更新追踪块时，应回到设计文档和开发文档的相关最小级标题，记录真实代码路径、函数名、测试名和验证命令。
3. 若一个功能对应多个小节，应分别更新所有相关小节；无法确认实现位置时必须标注“待确认”或“未实现”，不得伪造代码位置。
4. 追踪块必须记录完整项目内业务依赖，并为每个代码位置说明职责；不要只堆路径或行号。
5. 验证命令必须来自本轮真实执行，或明确标注尚未执行及原因。

## 环境执行规则

1. 当前开发与验证统一使用 conda 环境 `MultiCenterWSIGenerator`。
2. 运行项目命令时优先使用 `conda run -n MultiCenterWSIGenerator ...`，不要再用 `mamba run -n MultiCenterWSIGenerator ...` 生成新的验证记录。
3. 当前机器上 `mamba env list` 和 `conda env list` 中的 `MultiCenterWSIGenerator` 指向同一个 prefix；不要为了“删除 mamba 环境”而删除该共享环境目录，除非用户明确确认要移除整个 conda 环境。

## 仓库开发要求

1. 开发前维护 `docs/DEMANDS.MD`，把最新需求置顶记录。
2. 每次功能更新或迭代分配唯一语义化版本号，格式为 `v{major}.{minor}.{patch}`。
3. 版本号必须同步到 `VERSION`、`pyproject.toml`、代码常量、默认配置、测试断言和相关文档。
4. 每次改动同步更新 `README.md` 与 `docs/CHANGELOG.md`；日志按最新在前记录日期、用户需求、已做改动、影响文件和验证结果。
5. 优先使用 conda 或隔离环境安装开发依赖，不直接污染主环境。
6. 完成交付时确保 README 可让用户快速了解环境配置、使用方法、功能和当前边界。

## 并行开发要求

1. 使用 `.agent/tasks/`、`.agent/reports/` 和 `.agent/logs/` 跟踪 worker 任务、报告和日志。
2. worker 应使用独立 git worktree 与 `worker/<task-slug>` 分支。
3. 只把文件范围不重叠、验证命令可独立运行的任务并行化。
4. orchestrator 必须审查 worker report、`git status --short` 和 diff，并重新运行关键验证后再集成。
5. README、DEMANDS、CHANGELOG、audit checklist 等共享文档优先由 orchestrator 统一整理。

## 输出要求

完成任务后用中文以 `✅` 标识任务清单，明确已完成、部分完成或未完成项。不得伪造验证结果、隐藏失败或把历史工件说成本轮新验证。
