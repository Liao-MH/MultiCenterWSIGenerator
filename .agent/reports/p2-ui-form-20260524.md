# Worker 最终报告：p2-ui-form-20260524

## 状态

- 结果：`完成`
- Worker 分支：`worker/p2-ui-form`
- Worker worktree：`.worktrees/p2-ui-form`
- Commit：`00018e9` 基线后未提交

## 摘要

将 `create_main_window(config=None)` 从只读字段展示替换为可交互 PySide6 单页表单。表单支持 generation config 保存路径、run-generation 必填参数、backend、生成参数、可选 condition packet / training index、QC non-copy checkbox 和 6 类 label mapping 输入。保存配置时会校验 generation config schema、数值边界和 label mapping；创建任务时会先校验必填运行参数，再写 config 并通过现有 `JobRunner` 创建 queued job，不执行生成核心逻辑。

## 影响文件

- `src/he_wsi_generator/ui/pyside_app.py`：新增 PySide6 表单窗口、保存 generation config、run command/job 创建和 UI 状态区错误展示。
- `tests/test_ui.py`：新增 PySide6 offscreen 表单测试；在本测试文件中将当前 worktree 的 `src` 放到 `sys.path` 前面，避免当前 conda 环境的 editable install 指向相邻 worker worktree。
- `.agent/reports/p2-ui-form-20260524.md`：本 worker 报告。

## RED 阶段失败测试命令和失败原因

Command:

```bash
QT_QPA_PLATFORM=offscreen mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_ui.PySideFormTests -v
```

Result: exit code `1`

Failure reasons:

- `test_main_window_exposes_enabled_form_controls`：找不到 `generation_config_path_input` 等表单控件，旧窗口仍是只读骨架。
- 其余 PySide 表单测试因缺失控件出现 `AttributeError: 'NoneType' object has no attribute 'setText'`。
- 首次运行还暴露当前 conda 环境的 editable install 指向 `.worktrees/p2-ui-job-command`，因此测试文件已显式优先加载当前 worktree `src`。

## 验证

| 命令 | 结果 | 证据 |
|---|---|---|
| `QT_QPA_PLATFORM=offscreen mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_ui -v` | `通过` | `Ran 22 tests in 0.217s`；`OK` |
| `mamba run -n MultiCenterWSIGenerator python -m unittest tests.test_version -v` | `通过` | `Ran 2 tests in 0.000s`；`OK` |
| `git diff --check` | `通过` | exit code `0`，无输出 |

## 需求覆盖

- 确认 worktree/branch 与必读文件：`完成` — 当前路径为 `.worktrees/p2-ui-form`，分支为 `worker/p2-ui-form`，必读文件均存在并已读取相关内容。
- 真实 PySide6 单页表单：`完成` — 覆盖保存路径、prior/checkpoint/output/generated id、backend、anchor preset、structure anchor、seed、sample steps、overlap、condition packet、training index、QC non-copy 和 6 类 mapping 输入。
- 保存 generation config JSON：`完成` — 保存前校验 label mapping 与 `validate_generation_config()`，测试验证写出 JSON 可被 schema 接受。
- 创建运行命令/任务：`完成` — 使用现有 `JobRunner` 创建 queued job record；缺 prior manifest、空 generated id、torch backend 缺 training index 会阻塞并显示状态区错误。
- 保持 CLI-first 与 PySide 可用性行为：`完成` — 未修改 CLI；`run-generation` 不会默认弹 GUI；`ensure_pyside_available()` 仍显式抛出 `UIUnavailableError`。
- 最小 GUI 测试：`完成` — PySide6 可用时用 `QT_QPA_PLATFORM=offscreen` 创建窗口并验证控件、保存、非法 label mapping 和缺失运行参数阻塞。

## 风险与备注

- 本 worker 只实现表单层和 queued local job record，不执行后台队列、真实生成、路径存在性校验或 production backend 逻辑。
- Label mapping 当前作为 UI 输入校验项，不写入 generation config JSON；generation config 继续保持既有 schema 契约。
- 当前 v0.63.0 schema 仍拒绝 `non_copy_patch_nearest_neighbor_search=true`，因此用户勾选 QC non-copy checkbox 时会被显式阻塞。
- 当前 conda 环境的 editable install 曾指向相邻 worker；`tests/test_ui.py` 已优先加载当前 worktree `src`，但 orchestrator 合并前仍建议复核环境 editable 路径或重新安装当前仓库。

## 阻塞项

- 无。

## Orchestrator 后续动作

- 审查并合并 worker diff。
- 如进入 v0.64.0 收口，由 orchestrator 统一同步版本号、README、`docs/DEMANDS.MD`、`docs/CHANGELOG.md` 和审计清单；本 worker 按任务约束未修改这些文件。

## Diff 摘要

```text
 src/he_wsi_generator/ui/pyside_app.py | 309 +++++++++++++++++++++++++++++++---
 tests/test_ui.py                      | 148 +++++++++++++++-
 2 files changed, 434 insertions(+), 23 deletions(-)
```
