# Worker 最终报告：<task-id>

## 状态

- 结果：`<完成 | 部分完成 | 阻塞 | 失败>`
- Worker 分支：`worker/<task-slug>`
- Worker worktree：`.worktrees/<task-slug>`
- Commit：`<commit-hash 或 未提交>`

## 摘要

`<用 2-5 句话说明做了什么、为什么这样做>`

## 影响文件

- `<路径>`：`<简要说明用途>`

## 验证

| 命令 | 结果 | 证据 |
|---|---|---|
| `<命令>` | `<通过 | 失败 | 跳过>` | `<关键输出、失败原因或跳过原因>` |

## 需求覆盖

- `<需求项>`：`<完成 | 部分完成 | 未完成>` — `<证据>`

## 风险与备注

- `<风险或备注>`

## 阻塞项

- `<无或具体阻塞项>`

## Orchestrator 后续动作

- `<需要审查 | 需要合并 | 需要决策 | 无>`

## Diff 摘要

```text
<粘贴 git diff --stat 输出；若未生成则说明>
```
